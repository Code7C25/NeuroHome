name: neurohome
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.4
  http: ^1.5.0
  webview_flutter: ^4.4.2
  permission_handler: ^11.0.1
  
  # DEPENDENCIAS ESENCIALES para Node.js en Android:
  path_provider: ^2.1.1    # Para manejar directorios temporales
  process_run: ^0.13.0+2   # Para ejecutar procesos Node.js
  universal_io: ^2.2.2     # Para compatibilidad IO entre plataformas
  
  # Backend alternativo en Dart (como fallback):
  shelf: ^1.4.3            # Backend Dart simple
  sqflite: ^2.3.0          # Base de datos local
  shared_preferences: ^2.2.2 # Almacenamiento simple
  connectivity_plus: ^5.0.1 # Verificar conexión

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/nodejs/  # Incluye todos los archivos Node.js

    dependencies:
  flutter:
    sdk: flutter
  shelf: ^1.4.3
  shelf_router: ^1.1.3
  sqlite3: ^2.3.0
  dart_jsonwebtoken: ^3.0.1
  bcrypt: ^3.0.1
  # Tus dependencias existentes:
  flutter_secure_storage: ^9.2.4
  http: ^1.5.0
  webview_flutter: ^4.4.2
  permission_handler: ^11.0.1
  path_provider: ^2.1.1
  shared_preferences: ^2.2.2
  connectivity_plus: ^5.0.1